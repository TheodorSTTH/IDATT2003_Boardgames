package edu.ntnu.irr.bidata.Model.interfaces.observer;

/**
 * Is the observer in the Observer design pattern. Is meant for
 * the object observing another object
 * */
public interface IObserver {
  void update();
}
